<h1>Our Fleet</h1>
<div>
    <div class="float-container">
        {{#each cars}}
        <div class="fleet-container">

            <img src="/images/{{image}}.jpg">

            <div id="h2">
                <h2>{{title}}</h2>
            </div>
            <h3>{{description}}</h3>
            <h3>{{price}}</h3>
            <button><a href="/rent/{{carId}}">Book Now</a></button>
        </div>
        {{/each}}


    </div>
</div>